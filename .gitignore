# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/.next-*/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Lesson films ARE committed.
#
# They were ignored here once, and the result was a deployed site with exactly
# one working video — the only one that happened to be tracked. Until the films
# move to a CDN (set NEXT_PUBLIC_VIDEO_BASE_URL), they have to ship with the
# repo, because a deployment builds from what git holds and nothing else.
